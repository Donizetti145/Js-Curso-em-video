var n1 = Number.parseFloat(window.prompt('digite um numero'));
var n2 = Number.parseFloat(window.prompt('digite outro numero'));
var s = (n1 + n2);
alert(`A soma enter ${n1} e ${n2} é ${s}`);
// (number + number) para adição 
//  (string + string) para concatenação

// String -> Number
// parseInt(n) - Numero inteiro 
// parseFloat(n) - Números reais
// Number(n) - identificação automatica

// Number -> String
// String(n)

// template strin
// ${nome_da_variave}

// Formantando string